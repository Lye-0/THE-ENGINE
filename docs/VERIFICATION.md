# 検証記録

最終確認日: 2026-09-07

## 全面表示への変更後の確認

HTML・CSSの変更に対し、サイト構成・プレビュー生成のテスト4件と、既存のブラウザー検証13項目が成功しました。1440 × 960、390 × 844、820 × 1180、844 × 390で、キャンバスが表示領域全体と一致することを確認しています。拡大・移動時の端までの描画、文字と半透明パネルの前後関係、左端やヘッダーの空白部分からのドラッグ、部品解説・燃焼接写も確認しました。

## 自動テスト

`npm test` と同じNode.jsテストコマンドの結果: **52件成功、失敗0件、スキップ0件**。

Node.js v24.19.0で、以下を実行しました。

- スライダークランクの寸法・位相・行程・カムの回転比・入力検証。
- 生成形状の数値・属性・頂点インデックス、リングの面方向、行列、実モデルのコンロッド端点とピストンピンの一致。
- 実際のローカルHTTPサーバーのHTML／CSS／JS配信、HEAD、404、異常URL、パストラバーサル拒否、書き込みメソッド拒否。
- 分離ファイルの参照、外部通信依存がないこと、UIのID、JavaScriptの構文、単体プレビュー生成時のソース保全。
- カムの着座ランプ、曲率、バケット上の接触点、16個の実メッシュとバケット面の接触。
- 720°にわたる傾斜バルブの移動、ピストンとの隙間、スプリングの密着長と仮定した質量・予圧での接触力。
- 各気筒の圧縮側での点火、放電時間、吸気バルブが開いている間の噴射と噴霧移動。
- 中空管の形状、インジェクターから噴霧終端への接続、表示モードとエフェクトの整合性。
- 共通入口の開口、両噴射コーンの通過、ヘッド支柱・周辺部品による流路遮断がないことを実メッシュの交差で検証。正面・斜めの接写でも確認。

- 燃焼の着火遅延、火炎面の進行、冷却、4気筒の位相、角度巻き戻し、実際のピストン上面への追従、表示切り替えと照明停止。
- 傾斜天井を数値積分した燃焼室容積とガス密度の積が、閉じた膨張区間で一定になること。

## リアリティ改善後のブラウザー確認

WindowsのMicrosoft Edge 152.0.4191.62とNode.js版Playwrightで、`tests/browser.cjs` の **13項目すべて成功**。結果は `realism-browser-checks.json` に保存しています。

燃焼は0°・12°・24°・60°・105°で、LOW／STD／HIGHすべてのGPU画素を比較しました。火炎が広がること、既燃ガスが膨張すること、燃焼室の外やピストン表面へガスが描かれないことを確認しています。「燃焼」接写は320／390／820幅でも確認しました。

点火と噴射は、正しい気筒・位置で可視になっていることに加え、GPUが描いた該当領域の画素をエフェクト有無で比較しました。外観・断面・分解、8部品の解説、描画品質3段階、再生・停止、角度入力、RPM、再生速度、自動回転・リセット、説明ダイアログの停止／再開も確認しています。

燃焼の更新後も、赤い放電線と燃料噴射の描画を継続して確認しています。点火の画素比較は、比較前に明示的に描画し、照明を同じ状態に保って放電線だけの表示有無を検証します。点火の接写と320px幅の表示を実画面で確認しています。

1440 × 960、390 × 844、320 × 740、820 × 1180で観察操作を確認しました。新しいシェーダーのコンパイル、JavaScript、WebGLにエラーはありません。ブラウザーは本体と同じソースから生成した自己完結HTMLを `file:` URLで読み、HTTP配信は既存のサーバーテストで別途確認しています。

Node.jsの検証環境にPlaywrightとブラウザーがある場合は、以下で再実行できます。サイト本体は引き続き外部ライブラリに依存しません。

```text
npm run test:browser
```

`THE_ENGINE_BROWSER_CHANNEL=msedge` でインストール済みEdgeを選べます。`THE_ENGINE_PLAYWRIGHT_MODULE` は既存のPlaywrightモジュールへのパス、`THE_ENGINE_TEST_OUTPUT` はスクリーンショット・JSONの保存先を指定する任意の環境変数です。保存先を省略すると一時ファイルは検証後に削除します。

## ブラウザー操作（初期制作時の記録）

Chromium 144系、Linux、Xvfb＋SwiftShaderのWebGL2で **34項目成功**。実行結果を `browser-checks.json` に保存しています。

再生・停止、角度入力、回転数、時間倍率、燃焼色、外観／断面／分解、復帰時のチェーン表示、ドラッグ回転、ホイールズーム、平行移動、自動回転、視点リセット、2本指ピンチ、部品解説、ページ送り、Escape、音の有効化、ダイアログ、品質変更、WebGL未対応時の説明表示を確認しました。停止した視点を無駄に再描画し続けないことも確認しています。

通常ページのブラウザーコンソールにエラーはなく、確認時の `gl.getError()` は0でした。WebGL未対応のフォールバック試験では、試験用ページだけでコンテキスト取得を無効化しています。

## 画面サイズの確認（初期制作時の記録）

実際のWebGL描画とUIを、次のビューポートで確認しました。

| 画面 | 幅 × 高さ |
| --- | --- |
| デスクトップ | 1440 × 960 |
| 操作テスト | 1024 × 768 |
| スマートフォン縦 | 390 × 844 |
| タブレット縦 | 820 × 1180 |
| 横長・低い画面 | 844 × 390 |

スクリーンショットは生成AIの完成予想図ではなく、本体から描画した実画面です。GIFプレビューは、実装のクランク角を18°ずつ変え、720°の1サイクルを描画して作成しています。

## 確認環境の制約（初期制作時の記録）

この制作環境のブラウザーはURLへの移動が管理ポリシーで制限されています。そのためブラウザー操作テストには、分離した本体から `build-preview.mjs` で生成した**同一ソースの自己完結HTML**を使い、Playwrightの `set_content` で読み込みました。ポリシー自体は変更していません。

一方、静的ファイルのHTTP配信は、別のNode.jsテストで実際にサーバーを起動して検証しています。つまり、配信とブラウザー描画はそれぞれ確認済みですが、この環境ではURL移動から描画までを一続きに通したブラウザーテストではありません。

スマートフォン／タブレットは画面サイズと入力の確認であり、iPhone・iPad・Androidの実機テストではありません。Safari・Firefoxでの実機確認、GPUごとの性能測定、長時間の負荷テストは行っていません。

合成音についてはAudioContextの開始、実行状態、出力ゲインの変化、停止を検証しました。ヘッドレス環境のため、スピーカーから出た音の聴感評価を実施したという意味ではありません。全画面表示はブラウザーや埋め込み側の許可に依存し、利用できない場合はUIで知らせます。

## 任意のブラウザーテストの再実行

Python環境にPlaywrightとそのChromiumが導入されている場合:

```text
python tests/browser.py
```

この検証用Pythonスクリプトは本体の動作には不要で、`npm test` からも呼びません。別のChromium実行ファイルを利用する場合は `FERRO_BROWSER_EXECUTABLE` を指定できます。ソフトウェアWebGLによる検証は `FERRO_SOFTWARE_GL=1`、必要な隔離CI環境でのみサンドボックスを外す場合は `FERRO_NO_SANDBOX=1` を使用します。Linuxの構成によってはXvfbなどのディスプレイ環境も必要です。

本体を変更した後は、コードの自動テストに加え、実際に利用するブラウザー・端末で見た目と操作を確認してください。
